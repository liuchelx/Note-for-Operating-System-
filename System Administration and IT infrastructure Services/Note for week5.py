Data recovery: restaore data after unexpected lost

POST-mortem: record the problem and how to solve them

backup your data:
    backup solution:
        insite
        offsite
       
    rsync:
        use SSH synchronized file , often used as backup

    time machine:
        app back up solution
    
    test backup:
        disaster recoery testing

    type of backup:
        full backup
        differential backup: only backup the changes
        reguilar incremental backup: when only the data that changed since the last incremental backup

        file compression

    redundant array of independent DIsk(RAID): combine multiple disk as one virtual lagre disk



